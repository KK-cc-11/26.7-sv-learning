# C++从0开始

## 基本语法

### **1. iostream：** 格式化和类型安全的I/O(输入输出，准确定义是内存（CPU侧）与外设（磁盘、网卡、终端、USB等）之间的数据搬运过程)

#### ①阻塞 I/O（Blocking）：默认的 fread / std::cin。线程调用后直接挂起休眠，直到数据就绪。坏处：一个线程只能服务一个连接。

#### ②非阻塞 I/O（Non-blocking）：调用立即返回，告诉你“没数据”，你需要不断轮询。坏处：空转浪费 CPU。

#### ③I/O 多路复用（Reactor）：epoll / io_uring（Linux 王牌）。这是 C++ 高并发服务器的基石——用一个线程监听成百上千个 I/O 事件，哪个就绪处理哪个。

#### ④信号驱动 I/O：数据来了发信号通知你，实际工程极少用（信号处理复杂）。

#### ⑤异步 I/O（Proactor / 真正的 AIO）：你发起 read 后立即返回，内核把数据完全拷贝到你的用户态缓冲区后，再回调通知你。这是 I/O 性能天花板（如 Windows IOCP，Linux 最新的 io_uring）。

### `std::vector` 和原生数组（`int arr[N]`）彻底剖开。

#### 1. 内存栖身之所（最本质的差异）
这是两者最核心的底层鸿沟，直接决定了你的程序会不会爆栈：

-   **原生数组 `int arr[100]`**：**通常**栖息在**栈（Stack）**上。
    -   栈大小极其有限（Windows 约 1MB，Linux 约 8MB）。如果你写 `double big[1024][1024]`（约 8MB），程序会直接因**栈溢出（Stack Overflow）**崩溃。
    -   栈分配极快：仅仅是一条汇编指令 `sub rsp, 400`（移动栈顶指针），耗时近乎为 0 纳秒。
-   **`std::vector<int> vec(100)`**：对象本身在栈上（仅占 24 字节，存着 3 个指针），但**实际数据**通过 `new` / `malloc` **栖息在堆（Heap）**上。
    -   堆分配需要向操作系统申请，涉及链表查找和系统调用，耗时约在 100 纳秒~微秒级。
    -   堆几乎拥有全部内存容量（受限于虚拟内存），可以存放 GB 级数据。

---

#### 2. 编译器与语言层面的“身份”
-   **原生数组**：它是**裸内存块**。编译器只知道它的起始地址，**不知道**它有多长（`sizeof(arr)` 只在当前作用域有效，一旦作为参数传递，立刻退化为指针，丢失长度信息）。
-   **`std::vector`**：它是一个**类对象**。它自带 `size()` 和 `capacity()`，因为它的内存布局里藏着三个指针：`_Myfirst`（起始）、`_Mylast`（有效末尾）、`_Myend`（容量末尾）。

---

#### 3. 拷贝与赋值行为（极易出 Bug 的地方）
-   **原生数组**：**不能直接赋值**。`int b[10]; b = a;` 编译报错。拷贝必须手写 `memcpy` 或 `for` 循环，极其原始。
-   **`std::vector`**：**深拷贝**。`vector<int> b = a;` 会重新在堆上开辟一块内存，并拷贝所有元素。这保证了 RAII（资源获取即初始化）的安全，但如果你误以为它在“引用传递”，就会造成巨大的性能开销（所以传参时记得用 `const vector<T>&`）。

---

#### 4. 越界检查（安全底线）
-   **原生数组**：`arr[100] = 1;`（越界）——**不检查**。这叫“未定义行为（UB）”，可能 silently 覆盖了其他变量，也可能直接 Segment Fault。这是无数 C++ 内存漏洞的根源。
-   **`std::vector`**：`vec[100]` 同样不检查（为了性能）。但 `vec.at(100)` 会抛出 `std::out_of_range` 异常，给调试留了后路。

---

#### 5. 动态扩容（vector 独有的“成长痛”）
-   **原生数组**：大小固定，无法扩容。
-   **`std::vector`**：当 `size == capacity` 时再 `push_back`，会触发**重分配**。
    -   申请一块新内存（通常是旧容量的 **2 倍** 或 **1.5 倍**）。
    -   把旧元素移动/拷贝过去。
    -   释放旧内存。
    -   这是 vector 性能抖动的唯一来源。资深工程师会提前用 `reserve()` 预分配容量，避免多次重分配。

---

#### 6. 泛型与模板兼容性
-   **原生数组**：无法直接放进模板容器（除非用 `std::array` 或指针加长度）。
-   **`std::vector`**：完美支持 STL 算法（`std::sort`、`std::find`），有迭代器（`begin()`/`end()`），可以与范围 for 循环无缝衔接。

---

#### ⚠️ 资深工程师的终极决策指南

在真实的 C++ 工程项目中，我们的选择极其明确：

1.  **绝对不要用原生数组（除非写底层驱动或极简嵌入式）**。
    -   理由：容易越界、无法自动释放、无法拷贝、传参退化。
    -   C++ 之父 Bjarne Stroustrup 也多次强调：“`vector` 应该成为默认的容器。”

2.  **用 `std::array<int, 10>` 代替原生数组**。
    -   如果你确实需要在栈上分配固定大小的小数组（比如数学中的三维向量 `float[3]`），请用 `std::array`。它没有额外开销（内存布局完全等同原生数组），但拥有 `size()`、`at()`、迭代器，且可以作为函数参数传递而**不会退化**。

3.  **什么时候必须用原生数组？**
    -   只有一种情况：**与纯 C 的 API 交互**时（如 POSIX 的 `read`、`write`，或 OpenGL 的 `glBufferData`）。此时你可以用 `vec.data()` 获取底层指针，交给 C 函数，兼顾安全与性能。

---

**简单总结一句人话**：
`vector` 是“带保姆的堆内存管家”，`arr` 是“一块裸砖头”。现代 C++ 中，**除了与底层 C 库交互的那一瞬间，请永远把裸数组丢进历史的垃圾桶。**

如果你想深入探讨，我还可以给你展开两个方向：

-   **性能对比**：在 `-O2` 优化下，`vec[i]` 和 `arr[i]` 生成的汇编代码是否完全一致？
-   **移动语义**：如何利用 C++11 的移动构造，将 `vector` 的拷贝开销降为零？

既然你问到了栈和堆，说明你已经从“如何使用 C++”进入了“C++ 如何管理内存”的深水区。这是区分初级和资深工程师的一道分水岭——**不了解内存模型，就不可能写出高性能、无 Bug 的 C++ 代码。**

下面，我不讲虚的，直接从**硬件、操作系统、编译器**三个底层视角，把栈和堆彻底解剖给你看。

---

### 1. 物理本质：它们到底是什么？

-   **栈（Stack）**：一块由 CPU 硬件**直接支持**的线性内存区域。
    -   由**栈指针寄存器（RSP / ESP）** 维护。
    -   分配和释放，仅仅是一条 CPU 指令：`sub rsp, 48`（向下分配 48 字节）和 `add rsp, 48`（回收）。这速度极快，近似于 CPU 的寄存器操作。
    -   **内存布局**：后进先出（LIFO），严格连续。函数调用时压入（Push）栈帧，函数返回时弹出（Pop）。

-   **堆（Heap）**：一个由**操作系统内存管理器**管理的、离散的、链式的内存池。
    -   本质是一个**空闲链表（Free List）**或**红黑树**（取决于分配器算法，如 ptmalloc / jemalloc）。
    -   分配需要遍历这个链表，找到足够大的空闲块，分割并返回。释放时需要将块回收到链表中，可能涉及合并相邻空闲块。
    -   **内存布局**：不连续，碎片化严重。多个 `new` 分配的内存块在物理地址上可能相隔很远。

---

### 2. 生命周期管理（谁生谁死？）

| 维度 | 栈（Stack） | 堆（Heap） |
| :--- | :--- | :--- |
| **分配时机** | 编译期就确定了大小（函数帧大小固定）。 | 运行时动态决定（`new` / `malloc` 时才分配）。 |
| **释放时机** | 离开作用域（函数返回）时，**自动**、**立即**释放。 | 必须手动 `delete` / `free`，或者由智能指针（RAII）自动回收。 |
| **内存泄漏风险** | **几乎为零**。栈绝不会泄漏，因为系统强制回收。 | **极高**。忘记 `delete` 或出现异常未捕获，内存就永远被占用。 |
| **悬挂指针风险** | **极高**。函数返回后，栈帧被销毁，指向栈上变量的指针立刻失效（野指针）。 | 中等。只要不 `delete`，指针永远有效。 |

---

### 3. 容量上限（你在悬崖边跳舞吗？）

-   **栈**：**极其狭小**。
    -   Linux / Windows 默认一般 **1MB ~ 8MB**。
    -   **警告**：若在栈上定义 `int huge[1024 * 1024]`（约 4MB），一旦超出限制，直接触发 **Stack Overflow**，程序立刻崩溃，无任何挽回余地。
-   **堆**：**极其宽广**。
    -   受限于虚拟内存和物理 RAM + Swap。64 位系统下理论寻址空间达 16EB（实际受限于系统资源），分配几 GB 轻松自如。

---

### 4. 速度差异（微观基准）

我们来一组真实的比例（纳秒级）：

-   **栈分配**：约 **1 ~ 3 纳秒**。仅仅是移动指针。
-   **堆分配**：约 **50 ~ 200 纳秒**（小对象）甚至 **微秒级**（大对象）。
    -   这个时间包括：进入内核态、遍历空闲链表、加锁（多线程场景）、清空内存（如果调用 `calloc`）、返回。

**而在多线程环境下**，堆分配还会因为**互斥锁**争用而产生惊人的性能退化。这也是为什么高性能 C++ 服务器（如 Facebook 的 Folly 库）会实现**线程局部分配器（TLS allocator）**，让每个线程拥有独立的堆，避免锁竞争。

---

### 5. 内存碎片（堆的隐形杀手）

-   **栈**：完美连续，无碎片。
-   **堆**：频繁 `new` / `delete` 不同大小对象，会产生**外部碎片**（空闲块散布在各处，总和够，但单块不够分配，导致分配失败或触发内存紧缩）。

---

### 6. 分配与释放的隐藏开销（你以为的 `new` 只是个 `new`？）

当你写下 `int* p = new int(5);` 时，背后发生了：

1.  **调用 `operator new`**（它内部调用 `malloc`）。
2.  `malloc` **可能**触发 `brk` 或 `mmap` 系统调用（陷入内核）。
3.  如果内存池有空闲块，则返回；否则向 OS 申请扩大堆顶。
4.  **如果对象有非平凡的构造函数**，编译器会在分配的内存上调用构造函数。

而 `delete` 时：先调用析构函数，再调用 `operator delete`（内部调用 `free`），将内存块归还给堆管理器。

**相比之下，栈上的对象构造**：在函数入口处一次性分配栈帧空间，然后按顺序调用构造函数。析构时反向调用，无需向 OS 归还——仅仅是 `RSP` 指针复位。

---

### 7. 调试与排查（资深工程师的硬功夫）

-   **栈溢出检查**：编译时加 `-fstack-protector-strong`，运行时用 `Valgrind` 或 AddressSanitizer（ASan）检测。
-   **堆内存泄漏**：用 `Valgrind --leak-check=full`，或在代码中引入 `mtrace`。
-   **堆溢出 / UAF（Use-After-Free）**：ASan 可以精准捕获到访问已释放内存的行号。

---

### ⚠️ 资深工程师的决策铁律（直接抄作业）

| 场景 | 选择 | 理由 |
| :--- | :--- | :--- |
| **小于 1KB 的小对象**（如局部 `int`、`std::array`、小结构体） | **栈** | 零开销、自动管理、极速。 |
| **大于 1KB 的大数组/大对象** | **堆** | 栈放不下，强行放会崩溃。 |
| **需要动态增长**（如 `std::vector`） | **堆** | 栈大小固定，无法扩容。 |
| **需要在函数间跨越生命周期返回** | **堆**（用智能指针 `std::unique_ptr` / `shared_ptr`） | 栈对象离开函数就销毁，必须用堆延长生命。 |
| **多线程高频分配小对象** | **自定义内存池 / TLS 分配器** | 绕过全局堆锁，性能提升数十倍。 |

---

### 最后，给你留个“灵魂拷问”作为延伸：

如果我在 C++ 里写：

```cpp
void func() {
    int* p = new int(42);
    // 没有 delete p;
}
```

这会导致**内存泄漏**（Heap 上的 4 字节永远回不来）。
但如果我把 `new` 换成 `int x = 42;` 并返回 `&x`，那叫**悬挂引用**，更危险（访问已销毁的栈内存）。

**那么问题来了**：如果我在函数内 `new` 了一个对象，然后把它**赋值给一个 `std::unique_ptr`** 并返回，这个对象是存在栈上还是堆上？它的生命周期如何管理？

如果你答得上来，说明你已经真正理解了栈和堆的配合逻辑。如果你还想深入了解 **“堆管理器是如何处理多线程竞争”** 或 **“栈帧里的返回地址和局部变量是如何排列的”**，告诉我方向，我继续给你深入拆解。🔧

# 注意：unsigned当数=0的时候再-1会变成一个大数，而不是一个负数！！！

